import { Search, ShoppingBag, Store } from 'lucide-react';
import { useEffect, useMemo, useState } from 'react';
import { Link } from 'react-router-dom';
import { apiClient, formatGHS } from '@my-app/shared';

const fallbackImage = 'https://images.unsplash.com/photo-1523275335684-37898b6baf30?auto=format&fit=crop&w=900&q=85';

export default function ModernMarketplace() {
  const [products, setProducts] = useState([]); const [search, setSearch] = useState(''); const [loading, setLoading] = useState(true); const [error, setError] = useState('');
  useEffect(() => { apiClient.get('/products/marketplace').then(({ data }) => setProducts(data.products || [])).catch(() => setError('The marketplace is taking a moment to load.')).finally(() => setLoading(false)); }, []);
  const visibleProducts = useMemo(() => { const query = search.trim().toLowerCase(); return query ? products.filter((product) => [product.title, product.description, product.storeName].some((value) => value?.toLowerCase().includes(query))) : products; }, [products, search]);
  return <main className="modern-surface min-h-screen"><header className="relative z-10 border-b border-white/10 bg-[#07111f]/70 backdrop-blur-xl"><div className="mx-auto flex max-w-7xl items-center justify-between gap-5 px-5 py-5 sm:px-8"><Link to="/marketplace" className="modern-logo">LinkPay<span>.</span></Link><div className="hidden items-center gap-7 text-sm font-bold text-slate-400 sm:flex"><span className="text-white">Discover</span><Link to="/profile" className="transition hover:text-cyan-300">My account</Link></div><Link to="/profile" aria-label="Open account" className="flex h-10 w-10 items-center justify-center rounded-xl border border-white/15 bg-white/10 text-white transition hover:border-cyan-300/50 hover:text-cyan-300"><ShoppingBag size={17} /></Link></div></header><section className="relative z-0 border-b border-white/10"><div className="mx-auto max-w-7xl px-5 py-16 sm:px-8 sm:py-24"><p className="modern-eyebrow">The LinkPay marketplace</p><h1 className="mt-5 max-w-3xl text-5xl font-black leading-[.93] tracking-[-.07em] text-white sm:text-7xl">Find the signal<br /><span className="text-cyan-300">worth following.</span></h1><p className="mt-6 max-w-xl text-lg leading-8 text-slate-300">A live collection of independent stores, sharp products, and things you did not know you needed.</p><label className="modern-input mt-9 flex max-w-2xl items-center gap-3 rounded-xl px-4 py-3.5"><Search size={19} className="text-cyan-300" /><input value={search} onChange={(event) => setSearch(event.target.value)} className="w-full bg-transparent text-sm font-semibold outline-none" placeholder="Search products or stores" /></label></div></section><section className="relative z-10 mx-auto max-w-7xl px-5 py-12 sm:px-8 sm:py-16"><div className="flex items-end justify-between gap-4"><div><p className="text-sm font-bold text-slate-400">Curated in real time</p><h2 className="mt-2 text-2xl font-black tracking-tight text-white">Trending products</h2></div><span className="text-sm font-bold text-slate-500">{visibleProducts.length} items</span></div>{loading && <p className="py-24 text-center text-sm font-bold text-slate-400">Scanning the marketplace...</p>}{!loading && error && <div className="py-24 text-center"><p className="font-bold text-slate-300">{error}</p><button type="button" onClick={() => window.location.reload()} className="modern-button modern-button-primary mt-5">Try again</button></div>}{!loading && !error && visibleProducts.length === 0 && <div className="glass-panel mt-8 py-24 text-center"><Store className="mx-auto text-slate-500" size={40} /><p className="mt-4 font-bold text-slate-300">No products match that search yet.</p></div>}{!loading && !error && visibleProducts.length > 0 && <div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{visibleProducts.map((product) => <article key={product._id} className="modern-card group"><Link to={`/store/${product.storeSlug}/checkout/${product._id}`}><div className="aspect-square overflow-hidden bg-slate-900"><img src={product.imageUrl || fallbackImage} alt={product.title} className="h-full w-full object-cover" /></div><div className="p-4"><p className="truncate text-xs font-bold text-cyan-300">{product.storeName}</p><h3 className="mt-2 truncate text-base font-black text-white">{product.title}</h3><div className="mt-4 flex items-end justify-between"><p className="text-lg font-black text-white">{formatGHS(product.price)}</p><p className="text-[11px] font-bold text-slate-500">{product.inventoryCount} left</p></div></div></Link></article>)}</div>}</section></main>;
}
