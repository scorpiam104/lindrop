import { Camera, X } from 'lucide-react';
import { useEffect, useRef, useState } from 'react';

export default function QRScannerModal({ open, onClose, onScan }) {
  const videoRef = useRef(null); const [error, setError] = useState('');
  useEffect(() => { if (!open) return undefined; let stream; let timer; navigator.mediaDevices?.getUserMedia({ video: { facingMode: 'environment' } }).then((value) => { stream = value; if (videoRef.current) videoRef.current.srcObject = value; if ('BarcodeDetector' in window) { const detector = new window.BarcodeDetector({ formats: ['qr_code'] }); timer = window.setInterval(async () => { if (!videoRef.current || videoRef.current.readyState < 2) return; const codes = await detector.detect(videoRef.current); if (codes[0]?.rawValue) { onScan?.(codes[0].rawValue); onClose?.(); } }, 500); } }).catch(() => setError('Camera permission is required to scan proof of delivery.')); return () => { window.clearInterval(timer); stream?.getTracks().forEach((track) => track.stop()); }; }, [open, onClose, onScan]);
  if (!open) return null;
  return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/80 p-4"><section className="w-full max-w-lg rounded-2xl bg-white p-5"><header className="flex items-center justify-between"><div className="flex items-center gap-2"><Camera className="text-[#2563EB]" size={20} /><h2 className="font-black">Scan proof of delivery</h2></div><button onClick={onClose} aria-label="Close" className="rounded-lg p-2 hover:bg-slate-100"><X size={18} /></button></header><div className="relative mt-5 aspect-square overflow-hidden rounded-xl bg-slate-950"><video ref={videoRef} autoPlay playsInline className="h-full w-full object-cover" /><div className="absolute inset-10 rounded-xl border-2 border-[#F59E0B]" /></div>{error ? <p className="mt-4 rounded-xl bg-red-50 p-3 text-sm font-bold text-red-700">{error}</p> : <p className="mt-4 text-sm text-slate-500">Point the camera at the customer’s delivery QR code.</p>}<button type="button" onClick={() => onScan?.('camera-scan-pending')} className="mt-4 w-full rounded-xl bg-[#1E3A8A] py-3 text-sm font-black text-white hover:bg-[#2563EB]">Confirm scan</button></section></div>;
}